# =================================================================
# INVOICES-BACK - VARIABLES DE ENTORNO
# =================================================================
# IMPORTANTE: Copiar este archivo como .env y rellenar con valores reales
# NO versionar el archivo .env (ya está en .gitignore)

# =================================================================
# POSTGRESQL - CREDENCIALES POR SERVICIO
# =================================================================
# User Service Database
USER_DB_HOST=localhost
USER_DB_PORT=5432
USER_DB_NAME=userdb
USER_DB_USERNAME=user_service_user
USER_DB_PASSWORD=CHANGE_ME_user_service_password_2025

# Invoice Service Database
INVOICE_DB_HOST=localhost
INVOICE_DB_PORT=5432
INVOICE_DB_NAME=invoicedb
INVOICE_DB_USERNAME=invoice_service_user
INVOICE_DB_PASSWORD=CHANGE_ME_invoice_service_password_2025

# Document Service Database
DOCUMENT_DB_HOST=localhost
DOCUMENT_DB_PORT=5432
DOCUMENT_DB_NAME=documentdb
DOCUMENT_DB_USERNAME=document_service_user
DOCUMENT_DB_PASSWORD=CHANGE_ME_document_service_password_2025

# Trace Service Database
TRACE_DB_HOST=localhost
TRACE_DB_PORT=5432
TRACE_DB_NAME=tracedb
TRACE_DB_USERNAME=trace_service_user
TRACE_DB_PASSWORD=CHANGE_ME_trace_service_password_2025

# =================================================================
# JWT CONFIGURATION
# =================================================================
# Clave secreta para firmar tokens JWT (mínimo 256 bits / 32 caracteres)
# Generar con: openssl rand -base64 32
JWT_SECRET=CHANGE_ME_your_super_secret_jwt_key_min_32_chars_base64_encoded_here
JWT_EXPIRATION_MS=3600000
JWT_ISSUER=invoices-backend

# =================================================================
# KAFKA CONFIGURATION
# =================================================================
KAFKA_BOOTSTRAP_SERVERS=localhost:9092
KAFKA_INVOICE_TOPIC=invoice-events
KAFKA_TRACE_GROUP_ID=trace-group

# =================================================================
# MINIO (S3-COMPATIBLE STORAGE)
# =================================================================
MINIO_ENDPOINT=http://localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=CHANGE_ME_minio_secret_key_2025
MINIO_BUCKET_NAME=invoices-pdfs
MINIO_REGION=us-east-1

# =================================================================
# CONFIG SERVER
# =================================================================
CONFIG_SERVER_URI=http://localhost:8888
CONFIG_SERVER_USERNAME=config-admin
CONFIG_SERVER_PASSWORD=CHANGE_ME_config_server_password_2025

# =================================================================
# EUREKA SERVER
# =================================================================
EUREKA_SERVER_HOST=localhost
EUREKA_SERVER_PORT=8761
EUREKA_USERNAME=eureka-admin
EUREKA_PASSWORD=CHANGE_ME_eureka_password_2025

# =================================================================
# CORS CONFIGURATION
# =================================================================
# Separados por coma, sin espacios
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,https://yourdomain.com
CORS_ALLOWED_METHODS=GET,POST,PUT,DELETE,OPTIONS
CORS_MAX_AGE=3600

# =================================================================
# SPRING PROFILES
# =================================================================
# Opciones: dev, test, prod
SPRING_PROFILES_ACTIVE=dev

# =================================================================
# LOGGING
# =================================================================
LOG_LEVEL_ROOT=INFO
LOG_LEVEL_APP=DEBUG
