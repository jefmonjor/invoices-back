# ⚠️ Archivo .env.production

## 🔒 Seguridad

El archivo `.env.production` contiene **secretos y credenciales** y por lo tanto:

❌ **NO debe ser commiteado a Git**
✅ **Está correctamente en .gitignore**
✅ **Debe permanecer solo en tu máquina local**

---

## 📝 Contenido

El archivo `.env.production` contiene:

- `JWT_SECRET` - Clave secreta para firmar tokens JWT
- `CORS_ALLOWED_ORIGINS` - URL del frontend en Vercel
- `USER_DB_URL` - Connection string de Neon (incluye credenciales)
- `INVOICE_DB_URL` - Connection string de Neon
- `DOCUMENT_DB_URL` - Connection string de Neon
- `TRACE_DB_URL` - Connection string de Neon

---

## 🔧 Cómo Crear

Si no tienes el archivo, créalo basándote en `.env.production.example`:

```bash
# Copiar template
cp .env.production.example .env.production

# Editar con tus valores reales
nano .env.production
```

---

## ✅ Verificación

Para verificar que está correctamente excluido:

```bash
# NO debe aparecer en git status como "Changes to be committed"
git status

# Debe aparecer en .gitignore
grep .env.production .gitignore
```

---

## 🚀 Uso en Deployment

Los valores de `.env.production` se configuran como **secrets en Fly.io**:

```bash
cd gateway-service
./deploy-to-flyio.sh
```

El script automáticamente:
1. Lee `.env.production`
2. Configura los secrets en Fly.io
3. NO los commitea a Git

---

## 📚 Más Información

Ver: `READY_TO_DEPLOY.md` para instrucciones completas.
